import { Link } from "react-router-dom";

const Register = () => {
    return (
        <div>
            <h1>register psle</h1>
            <Link to='/login'> Log In </Link>
        </div>
    );
};

export default Register;